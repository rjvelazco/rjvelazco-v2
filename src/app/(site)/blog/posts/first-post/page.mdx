import MdxLayout from '@components/mdx-layout'

export const metadata = {
  title: 'First Post',
  description: 'My first blog post using Next.js and MDX',
  slug: 'first-post',
  publishDate: '2024-04-09',
  summary: 'Discover why Vim, with its steep learning curve, remains a beloved tool among developers for editing code efficiently and effectively.',
  category: 'Development',
}

export default function MDXPage({ children }) {
  return <MdxLayout>{children}</MdxLayout>
 
}

# Hello, MDX!

This is my first blog post using **MDX** with Next.js.

## Why MDX?

MDX allows you to use JSX in your markdown content. This means you can:

- Import components
- Use React components directly in your content
- Apply dynamic behavior to your content

```javascript
// Example code block
function HelloWorld() {
  return <h1>Hello, World!</h1>;
}
```

You can even include interactive components in your blog posts!
